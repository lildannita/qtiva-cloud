## ✅ Уже реализовано

| Компонент | Статус |
|-----------|--------|
| Структура проекта | ✅ |
| Миграции БД | ✅ |
| JWT авторизация | ✅ |
| POST /auth/register | ✅ |
| POST /auth/login | ✅ |
| GET /me | ✅ |
| Seed admin CLI | ✅ |
| POST /admin/clients | ✅ |
| POST /admin/invites | ✅ |
| PATCH /admin/clients/{id} | ✅ |
| POST /artifacts | ✅ |
| POST /runs | ✅ |
| GET /runs/{id} | ✅ |
| GET /runs/{id}/log | ✅ |

## ❌ Осталось реализовать

### Manager (HTTP API)

| # | Задача | Приоритет | Сложность |
|---|--------|-----------|-----------|
| 1 | **GC (сборщик мусора)** — удаление данных после delete_after | Высокий | Средняя |

### Agent (воркер)

| # | Задача | Приоритет | Сложность |
|---|--------|-----------|-----------|
| 2 | **Базовый worker loop** — polling jobs из БД с lease | Высокий | Средняя |
| 3 | **Docker интеграция** — запуск контейнеров, сбор результата | Высокий | Высокая |
| 4 | **Конфигурация agent** — чтение настроек из env | Средний | Низкая |
| 5 | **Контроль таймаутов** — docker stop/kill при превышении | Средний | Средняя |

### Runner-образы

| # | Задача | Приоритет | Сложность |
|---|--------|-----------|-----------|
| 6 | **Dockerfile для runner** — manjaro + Qt 5.15 + X11/Wayland | Высокий | Средняя |
| 7 | **Скрипт /qtiva/runner** — распаковка, запуск, сбор логов | Высокий | Средняя |

### Опционально (можно упростить для MVP)

| # | Задача | Приоритет |
|---|--------|-----------|
| 8 | gVisor (runsc) runtime | Низкий (можно без него для MVP) |
| 9 | Wayland headless (можно начать с X11) | Низкий |
